resnet_src = [
    meson.current_source_dir() / 'resnet.cpp',
    meson.current_source_dir() / 'jni' / 'prelu_layer.cpp',
]

resnet_inc = include_directories('.', 'jni')

quick_ai_src += resnet_src
quick_ai_inc += resnet_inc
